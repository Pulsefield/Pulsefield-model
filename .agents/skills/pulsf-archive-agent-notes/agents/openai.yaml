interface:
  display_name: "Pulsefield Archive Agent Notes"
  short_description: "Classify and retire stale decision notes."
  default_prompt: "Use $pulsf-archive-agent-notes to classify and archive these Pulsefield decision notes without losing durable rationale."
