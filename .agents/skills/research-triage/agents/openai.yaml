interface:
  display_name: "Research Triage"
  short_description: "Triage research without inferring execution."
  default_prompt: "Use $research-triage to compare close analogues and hypotheses, then propose or evaluate a bounded experiment without inferring acceptance or run authority."
