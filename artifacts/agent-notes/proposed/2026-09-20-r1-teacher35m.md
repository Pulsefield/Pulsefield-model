# Agent Note: 35M teacher feasibility and later distillation

Note ID: 2026-09-20-r1-teacher35m
Status: proposed
Kind: research
Created: 2026-09-20
Updated: 2026-09-20
Product revision: db0a9b8b1a24d481e18bbb40e1a1266bf885756b
Scope: Bounded real-data training feasibility of a35.18M R1 teacher on the local24GiB Mac; no quality or distillation result
Related: 2026-09-20-vacation-compute-portfolio; 2026-09-20-r1-stage-quality-assessment

## Direction and authority

The user selects a35M teacher followed by distillation. The active goal separately
authorizes model changes and bounded execution. Keep this Note proposed, with no
Card acceptance implied. The existing response6.75M remains the quality baseline;
no output or inherited checkpoint is replaced. Only the engineering preflight is
selected here, not an unbounded pretraining run or student distillation.

Closest analogue for the later phase is Hinton et al.'s soft-distribution knowledge
distillation (https://arxiv.org/abs/1503.02531), with student-native prefix coverage
motivated by on-policy sequence distillation (https://arxiv.org/abs/2306.13649).
Those mechanisms transfer; quality and capacity requirements must be measured in
this exact timed-row task. Source-bounded novelty assessment: task adaptation,
not a proposed new distillation algorithm. The present preflight changes only
width and execution size and makes no learning-novelty claim.

## Experiment Card: r1-teacher35m-engineering-preflight-v1

Revision1, proposed, acceptance none. Question: can the existing R1 complete-row
model with512-wide local backbone,8levels,expansion4,coupling_rank16,observed
persistent seed and256-wide landmark memory at64H stride perform real full-history
forward/backward/AdamW updates and exact CPU recovery within the local resource
budget? No head/release/response residual is enabled. Parameter count35,178,768.
Do not infer native quality from this test or train on VAL/TEST.

Baseline source is cleandb0a9b8b1a24d481e18bbb40e1a1266bf885756b in an isolated
codex/r1-teacher35m worktree. Use the fixed TRAIN plan
SHAa613d4b77839e181c6474ba01e9e1a7d275efe35322608ac25c3e24f9853dc00,
with its admitted source cache. Choose one128-onset seed crop, one256-onset seed
crop from the first adequate plan source, and two128-onset deep-history crops
from the first128 plan draws having the largest first_onset values. Freeze exact
source/crop identities before model computation. The latter are workload stress
samples, not a corpus-throughput estimate.

Run CPU1 and MPS sequentially, each in a fresh subprocess, seed20260925,FP32,
AdamW lr.0003/weight_decay.01,clip1,microbatch1. Perform two full cycles of the four
fixed crops, keeping the second cycle separate from cold shape initialization.
Record complete preparation/forward/backward/update times, source-onset throughput,
RSS, physical footprint, MPS active/driver counters and swap growth at phase
boundaries. Compare fixed-state first-crop CPU/MPS source probabilities and losses
within2e-4 absolute/2e-4 relative on supported actions before updates. These are
platform numerical tolerances, not a scientific quality metric.

On CPU additionally save a compact model/optimizer/RNG snapshot, do one further
fixed-crop update, restore and repeat it. Require identical CPU parameters,
optimizer values,loss andRNG at the same continuation boundary. Use one checkpoint
at a time and discard in-memory graphs before saving. On both devices check finite
losses, gradients and final parameters, at least one changed weight, and no
mechanical support mismatch. The standard small-model TrainConfig envelope is
not silently weakened: this is a direct-model engineering probe; a runnable larger
training/generation profile remains separate work after evidence.

Resource bounds:600seconds per device,1,400seconds total,12GiB RSS/footprint,
8GiB MPS driver/allocator,512MiB swap growth,at least4GiB available memory,
100GiB disk reserve,2GiB fresh artifacts and1GiB single checkpoint. Stop immediately
on resource/nonfinite/identity failure and retain failure metadata. No retry,
scientific resume,source reselection or architecture switch. The goal is a feasible
measured path: either device may provide it, while any failed device remains an
explicit limitation. Both device failures require engineering refinement before
long training. Do not present extrapolated pilot rates as a days-long guarantee.

Artifact owner is artifacts/bounded-typed-continuation/teacher35m-preflight-20260920-v1/
in the teacher worktree. Run with uv run --offline --python3.10 --extra mps python
and a pinned driver after confirming the clean source OID and freezing selection.
A separate pretraining Card must set actual exposure,checkpoints,validation and
long-run guards from these measurements before the vacation worker is started.

### Selection and execution freeze

Four fixed crops have147/307/133/128 physical supervised rows and full BOS
histories170/330/3,607/3,222 rows. They represent128/256/128/128 source onsets.
Selection SHAc33c1135e4856ab41d7c3b61de75f4312d9898eb96c58e6153b490ac664699e2.
Before model computation, correct the probe's optimizer digest to byte-view scalar
step tensors through a flattened view. Retain the original preparation freeze
ad9a6c90903b2c51b4dcef0a2de548933be04b727557013f654e1c943335da95;
a separate execution-freeze.json binds that preparation, unchanged selection and
corrected driver. This is inspection-only digest handling, not an objective,
source,configuration or budget change. CPU and MPS then run sequentially under
the fixed clean source and resource guards. No long pretraining job is running.
